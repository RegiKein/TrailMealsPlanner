namespace TrailMealsPlanner.Domain.Enums;

/// <summary>
/// Represents altitude exposure because elevation affects energy demand and cooking conditions.
/// </summary>
public enum AltitudeRange
{
    Low = 0,
    Medium = 1,
    High = 2,
    Extreme = 3
}
